import React, {
    Dispatch,
    SetStateAction,
    useState,
} from 'react';

interface GSheetFormProps {
    setConnection: Dispatch<SetStateAction<null>>
}

const GSheetForm = (props: GSheetFormProps) => {

    return (
        <div>

        </div>
    );
}

export default GSheetForm;